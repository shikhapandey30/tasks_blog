<%= form_for([blog, comment, reply], remote: true) do |f| %>
 <%= f.hidden_field :comment_id, :value=> comment.id %>
 <%= f.text_field :body %>
 <%= f.submit  "Reply", class: "btn btn-primary" %>
<%end%>