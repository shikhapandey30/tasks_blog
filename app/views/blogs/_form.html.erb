<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>
      <ul>
        <% blog.errors.full_messages.each do |message| %>
         <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <table border="2" align="center" style="background: linear-gradient(to bottom, #33ccff 0%,
      #ff99cc 100%)" width="20%" height="100px">
      
      
      
      
    <tr align="center">
      <td align="center">
        <div class="field">
         <%= form.label :title %>
         <%= form.text_field :title %>
        </div>
      </td>
      <td>

         <div class="field">
          <%= form.label :image %>
          <%= form.file_field :image %>
        </div>
      </td>
      <td>
        <div class="field">
         <%= form.label :category %>
         <%= form.select(:category, Category.all.collect {|u| [u.name, u.id]}, :prompt => 'Select') %> 
        </div> 
      </td>
      <td>
        <div class="field">
         <%= form.label :description %>
         <%= form.text_field :description, :cols => 30, :rows => 30 %>
        </div>
      </td>
    </tr>
  </table>
  <br>
  <table>
  <tr>    
        <div class="actions" align="center">
         <%= form.submit "Create Blog", class: "btn btn-primary" %>
        </div>
      
    </tr>
  </table>
<% end %>